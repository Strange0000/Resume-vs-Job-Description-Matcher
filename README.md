A simple Python program that compares a resume with a job description
and calculates a match score based on keyword overlap.

## Features
- Text preprocessing (lowercase, stopword removal)
- Keyword extraction
- Match score (0–100)
- Matching and missing keywords

## How to Run
```bash
python main.py
